// The apps-root anchor. The gate requires one file per root that is always in scope, so a root that went dark cannot be hidden by the other root's file count.
export const boot = (): void => undefined;
