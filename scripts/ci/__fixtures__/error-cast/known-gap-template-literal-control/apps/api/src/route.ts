// The control half of the known-gap pair: the sibling tree's file with its template literal removed and nothing else changed, so the cast still starts on line 4.
export const doc = 'no template literal here';

export const bad = (err as Error).message;
export const closer = '*/';
