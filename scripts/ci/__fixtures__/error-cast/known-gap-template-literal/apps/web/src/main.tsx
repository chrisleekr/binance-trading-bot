// The apps-root .tsx anchor. Pins the extension widening itself: with .ts anchors only, dropping `.tsx` from the walk still satisfies every floor and every anchor while the gate examines zero .tsx files.
export const mount = (): void => undefined;
