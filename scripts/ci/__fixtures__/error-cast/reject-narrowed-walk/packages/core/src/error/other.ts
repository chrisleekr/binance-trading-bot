// A real file under packages, but not the anchor: this root is populated, so only the per-root anchor check can catch that it no longer reaches the module the rule points people at.
export const unrelated = 1;
