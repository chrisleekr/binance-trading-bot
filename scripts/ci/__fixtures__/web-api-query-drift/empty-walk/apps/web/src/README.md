The web root exists and the walk reaches it, but it holds no runtime source. That is the zero-file stop, and it is a different failure from the narrowed walk next door: there the walk still returns files and merely stops reaching an anchor. Both exit 1, so only the sentence tells them apart.

A Markdown file rather than an empty directory, because git does not track empty directories and the fixture would vanish on a fresh checkout, taking the case with it.
