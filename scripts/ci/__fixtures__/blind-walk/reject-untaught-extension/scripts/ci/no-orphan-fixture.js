// Fixture stub: a gate in a language this meta-gate has no recogniser for, deciding a verdict from a raw listing.
const { readdirSync } = require("node:fs");
if (readdirSync(".").length === 0) process.exit(1);
