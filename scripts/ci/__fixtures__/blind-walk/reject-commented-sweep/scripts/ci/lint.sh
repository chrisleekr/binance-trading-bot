#!/usr/bin/env bash
# Fixture stub whose sweep is commented out, so it satisfies a substring test and clears nothing.
# unset GUARD_ROOT WALK_GATE_MANIFEST MIGRATIONS_DIR
bash "$(dirname "$0")/no-locks.sh"
