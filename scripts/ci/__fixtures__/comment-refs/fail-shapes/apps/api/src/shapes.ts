// A parenthesised keyword form: the strategy registry check (issue #407) is one.
export const A = 1;

// A different keyword in the same shape (tracker #267) reads as prose.
export const B = 2;

// An unparenthesised epic #561 carries no brackets at all.
export const C = 3;

// #496 combined two operator flows, and opens the line with no bracket.
export const D = 4;

// A number sharing its brackets with other text (#534, audit F6) closes on a comma.
export const E = 5;
