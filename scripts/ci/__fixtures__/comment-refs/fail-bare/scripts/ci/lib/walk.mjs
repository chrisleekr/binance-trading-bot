// Scaffold. The .mjs anchor. The gate imports the REAL helper from $root and never from $GUARD_ROOT, so this copy is inert: a fixture tree must not be able to supply the walk stops it is being tested against.
export const WALK = true;
