// The packages root still returns a file, so the zero-file floor is satisfied and the count looks healthy. Its anchor, packages/contracts/src/decimal.ts, is absent: that is the narrowing only the anchor stop can see.
export const OTHER = true;
