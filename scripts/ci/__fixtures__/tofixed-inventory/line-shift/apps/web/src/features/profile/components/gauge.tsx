// A comment added above the site. The inventory keys on normalised line TEXT,
// never on line numbers, so an edit anywhere above must not churn the pin.



export const pct = (n: number): string => `${n.toFixed(2)}%`;
