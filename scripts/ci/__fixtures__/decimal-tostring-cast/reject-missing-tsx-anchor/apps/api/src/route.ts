/**
 * A multi-line block so the stripper has something real to blank.
 * `x.toString() as DecimalString` in prose is not a violation.
 */
export const glob = 'src/**/*.ts';
export const ok = asDecimalString(sum);
export const rebrand = row.profit as DecimalString;
