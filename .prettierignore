node_modules
dist
build
coverage
.turbo
playwright-report
test-results
**/tsconfig.tsbuildinfo
bun.lock

# Generated by scripts/docs/gen-config-tables.ts; compact tables, not prettier-managed.
docs/_generated/

# CI-gate fixtures carry deliberate admonition (in)correct indentation; prettier
# would normalise the body indent and break the no-broken-admonition self-test.
scripts/ci/__fixtures__/

# Per-agent instruction files are symlinks → AGENTS.md (the one real, formatted file).
# prettier 3 hard-errors on an explicitly-passed symlink, which breaks the lint-staged
# *.md hook; ignore the symlinks so only AGENTS.md is formatted.
/AGENT.md
/CLAUDE.md
/GEMINI.md
/.cursorrules
